all:
  hosts:
    localhost:
      ansible_connection: local
  children:
    nodes:
      hosts:
        node1: { ansible_host: 10.0.5.10 }
        node2: { ansible_host: 10.0.5.11 }
        node3: { ansible_host: 10.0.5.12 }
      vars:
        ansible_user: ubuntu
        ansible_ssh_private_key_file: ~/.ssh/id_rsa